import styled from 'styled-components';

export const Wrapper = styled.div`
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 20px;
    background: #f3f3f3;
    border-radius: 8px;
    box-shadow: 0px 4px 9px 4px rgba(34, 60, 80, 0.2);
`;

export const Inner = styled.div`
    flex-basis: 100%;
    flex-direction: column;
    display: flex;
    gap: 16px;
`;
